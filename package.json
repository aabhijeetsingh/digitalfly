{
  "name": "fly-connectome-single-file",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "lint": "echo 'No linting needed for single-file HTML app'"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
